
#include <stdio.h>
int main(){
   int length;
    int i , n,arr[length];
    printf("enter the length of array");
    scanf("%d",&length);
  for(i=0; i<length; i++){   
      printf("enter the %d inserting value",i);
      scanf("%d",&arr[i]);
    }
   for(i=0 ; i<length;i++){
     printf("the %d th arr values of the array");
     printf("%d ",[i]);
     printf("\n");
   }
return 0;
}